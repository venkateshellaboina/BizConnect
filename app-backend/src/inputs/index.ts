export {};
const BusinessInput = require('./business.input');
const UserInput = require('./user.input');
const LocationInput = require('./location.input');

const inputs = [BusinessInput, UserInput, LocationInput];

module.exports = inputs;